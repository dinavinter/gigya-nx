/**
 * Terminology:
 * // severity - describes the actual console method that has been invoked (severity 'info' means that someone has called gigya.logger.info).
 * // logLevel - describe the logger's configuration (logLevel 'debug' means that some has called gigya.logger.enable('debug'))
 */
