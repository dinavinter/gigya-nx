import { IPagesParams } from '../PagesPlugin';
import { RequireSessionPage } from './RequireSessionPage';
export declare class DeviceRegistrationPage extends RequireSessionPage {
    constructor(params: IPagesParams);
}
